# 客观题批改助手 UI 设计规范

本文件作为项目级设计约束使用，参考 `VoltAgent/awesome-design-md` 的工作方式：把设计规范放在项目根目录，后续改界面时优先遵守。

## 产品定位

这是给一线教师高频使用的批改工具，不是宣传页。界面应当安静、直接、可扫描，优先让老师快速完成“生成模板、录入答案、上传批改、查看成绩”。

## 界面原则

- 首屏直接进入可操作功能，不做营销式介绍。
- 少解释后台逻辑，只保留教师能立即理解的操作文字。
- 一个页面只突出当前最重要动作，例如“上传图片/文件”“保存标准答案”“打印模板”。
- 按钮文案使用教师口吻，避免“低成本版、服务器、HTTPS、本地浏览器”等技术词。
- 删除长期不推荐使用的旧流程，避免老师误选。
- 批改页默认只保留图片/PDF上传入口；手机端可通过同一上传入口选择拍照。

## 视觉风格

- 用浅底、深字、明确边框，保证投影仪和普通办公屏都能看清。
- 卡片圆角保持克制，不使用装饰性渐变、光斑或大面积插画。
- 底部 tab 顺序固定为：模板、标准答案、批改、成绩单、统计。
- 表单、按钮、上传区保持稳定尺寸，避免文字换行造成布局跳动。
- 识别结果中的红绿标记必须服务于核对，不做额外装饰。

## 可用性要求

- 模板页不要求先录入标准答案，只按题量生成模板。
- 上传前必须检查题量和标准答案数量是否一致，并用教师能理解的话提示。
- 任何会影响批改结果的设置都要显示当前状态，例如题量、填涂样式、名册人数。
- 移动端按钮要足够高，上传区要便于手指点击。
- 电脑端和手机端使用同一个正式网址，不再分发 HTML 文件。
